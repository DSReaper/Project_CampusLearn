<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Forgot Password</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;800&display=swap" rel="stylesheet">
  <link rel="alternate icon" href="/icons/favicon.ico" sizes="any">
  <link rel="stylesheet" href="/css/loginstyles.css">
</head>
<body>
  <!-- Animated code background -->
  <div class="code-bg" id="codeBg" aria-hidden="true"></div>

  <div class="wrap">
    <form class="card" id="forgotForm" novalidate>
      <h1>CampusLearn</h1>
      <p class="sub">Reset your password</p>

      <div class="field">
        <label for="email">Email</label>
        <input class="input" type="email" id="email" name="email" placeholder="you@company.com"/>
      </div>

      <div class="row" style="justify-content:flex-end">
        <a href="/">Back to login</a>
      </div>

      <button class="btn" type="submit">Send reset link</button>

      <!-- Success message (hidden by default) -->
      <p id="forgotSuccess" style="display:none; margin-top:16px; color:#000;">
        If an account exists for that email, we’ve sent a reset link.
      </p>
    </form>
  </div>

  <!-- Reuse your background + easter egg JS (safe: it checks elements before binding) -->
  <script src="/animation/loginanimations.js"></script>
  <!-- Tiny page-specific handler -->
  <script src="/animation/forgotpasswordanimations.js"></script>
</body>
</html>